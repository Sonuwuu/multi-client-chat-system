# multi-client-chat-system
A multi-client terminal-based chat application built using C socket programming and TCP connections. The project supports real-time messaging, private chats, timestamps, join/disconnect notifications, and multiple users on the same WiFi network using the select() system call without multithreading.
